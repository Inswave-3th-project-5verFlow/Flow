<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
	@subject     : 메뉴-그룹 정보 관련 처리를 담당하는 Sql Mapper
	@description : 메뉴-그룹 정보 관련 처리를 담당하는 Sql Mapper
	@author      : 백승호
	@since       : 2025/07/23
	@modification
	===========================================================
	DATE              AUTHOR             DESC
	===========================================================
	2025/07/23              백승호             최초 생성
 -->
<mapper namespace="com.demo.proworks.menu">
    <insert id="insertMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo">
    	<!-- 메뉴-그룹 정보를 등록한다. -->	
    		
        INSERT INTO MENU_GROUP_MAPPING   /* QueryID : com.demo.proworks.menu.insertMenuGroups */
        ( 

            <if test="menuId!= null"> , MENU_ID </if>
            <if test="grpId != null"> , GRP_ID  </if>
        )
        VALUES  
        ( 

            <if test="menuId!= null"> , #{menuId} </if>
            <if test="grpId != null"> , #{grpId}  </if> 
        )          
    </insert>	
	
    <update id="updateMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo">
    	<!-- 메뉴-그룹 정보를 갱신한다. -->
    	
        UPDATE MENU_GROUP_MAPPING    /* QueryID : com.demo.proworks.menu.updateMenuGroups */      
      	  SET  
        	                       
            <if test="menuId!= null"> , MENU_ID = #{menuId} </if>
            <if test="grpId != null"> , GRP_ID  = #{grpId}  </if>                 
        WHERE   

    </update>
	
    <delete id="deleteMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo">
    	<!-- 메뉴-그룹 정보를 삭제한다. -->
    	
        DELETE FROM MENU_GROUP_MAPPING   /* QueryID : com.demo.proworks.menu.deleteMenuGroups */ 
        WHERE   
        
    </delete>
	
    <select id="selectMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo" resultType="com.demo.proworks.menu.vo.MenuGroupsVo">
    	<!-- 메뉴-그룹 정보를 상세 조회한다. -->	
        SELECT     /* QueryID : com.demo.proworks.menu.selectMenuGroups */
            MENU_ID as menuId, GRP_ID as grpId
        FROM MENU_GROUP_MAPPING 
        WHERE 
			
    </select>
	
    <select id="selectListMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo" resultType="com.demo.proworks.menu.vo.MenuGroupsVo">
    	<!-- 메뉴-그룹 정보 목록을 조회한다. -->
           SELECT    /* QueryID : com.demo.proworks.menu.selectListMenuGroups */
               MENU_ID as menuId, GRP_ID as grpId		       
           FROM (
               SELECT  S.*, CEIL( (@ROWNUM:=@ROWNUM+1) / #{pageSize} ) AS PAGE
               FROM (				
                   SELECT 
                       *  		                  
                   FROM MENU_GROUP_MAPPING
                   WHERE  1=1    		
	     																														
               ) S, (SELECT @ROWNUM := 0) TMP
           ) A
            WHERE PAGE = #{pageIndex}
    </select>
	
    <select id="selectListCountMenuGroups" parameterType="com.demo.proworks.menu.vo.MenuGroupsVo" resultType="long">
    <!-- 메뉴-그룹 정보 목록의 카운트를 조회한다. -->
    
         SELECT    /* QueryID : com.demo.proworks.menu.selectListCountMenuGroups */
             COUNT(*) totcnt  		                  
         FROM MENU_GROUP_MAPPING
         WHERE  1=1    		

    </select>

</mapper>
